const baseImageURL = `${window.location.protocol}//${window.location.hostname}:3001`;
const baseApiURL = `${window.location.protocol}//${window.location.hostname}:3001/api`;

export { baseImageURL, baseApiURL };
